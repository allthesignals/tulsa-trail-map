import mapSources from 'tulsa-trail-map/utils/map-sources';
import { module, test } from 'qunit';

module('Unit | Utility | map-sources', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = mapSources();
    assert.ok(result);
  });
});
